class Card {

	constructor(shape, color, shade, amount) {
		// this.shapes = ['wave', 'diamond', 'ellipse'];
		// this.colors = ['red', 'purple', 'green'];
		// this.shades = ['full', 'lines', 'hollow'];
		// this.amounts = [1, 2, 3];

		this.shape = shape;
		this.color = color;
		this.shade = shade;
		this.amount = amount;
	}

	
}

module.exports = Card // Export class